# STM32F103C8T6 最小系统项目审计

审计时间：2026-07-29  
工程：`STM32F103C8T6_最小系统`  
原理图：`Schematic1 / P1`  
PCB：`PCB1`  
证据等级：`RULE_CHECK` + EasyEDA 画布/DRC 面板目视复核

## 结论

Decision: **FAIL**  
fabricationRelease: **false**  
manufacturingOutputsReviewed: **false**

本结果不是下单、投板或生产授权。

工程整体失败的直接原因包括严格原理图 DRC 未通过，以及 PCB
显式走线拓扑中存在未说明的 `3V3`、`GND` 闭环。PCB DRC 为零错误，
但晶振约束、未布线数量、板厂工艺、BOM/PnP、3D 机械检查和制造输出
尚未形成可核验的发布证据。

## 阻塞项

### 1. 原理图 DRC 有 7 条警告

- 4 条重复网络名：
  - `HSE_IN`：同一导线存在两个 `HSE_IN` 名称。
  - `HSE_OUT`：同一导线存在两个 `HSE_OUT` 名称。
  - `BOOT0`：同一导线存在两个 `BOOT0` 名称。
  - `NRST`：同一导线存在三个 `NRST` 名称。
- 1 条器件属性与供应商编号不匹配，涉及全部 17 个真实器件：
  `U1, R1, R2, R3, C1-C8, SW1, Y1, J1-J3`。
- 2 条未使用引脚缺少 No Connect 标识：
  - U1 共 32 个引脚：`2, 3, 4, 10-19（不含 20）, 21, 22, 25-33, 38-43, 45, 46`。
  - `SW1.4`。

建议先清理重复网络标号，确认每个未使用引脚的设计意图并放置 No Connect 标识，再重新运行严格原理图 DRC。

### 2. BOM 值属性不完整

DRC 信息项显示 12 个无源/晶振器件的“值”属性为空：
`R1-R3, C1-C8, Y1`。

画布和 API 的器件名称仍可读到 `10k`、`100nF`、`12pF`、`4.7uF`、`8MHz` 等文本，但应确认 BOM 实际使用的 Value 字段，并在导出前修正。

### 3. 晶振回路未满足发布证据门槛

检测到 `HSE_IN/HSE_OUT`，因此按 skill 要求启动晶振专项门槛。

已读回的几何数据：

- `U1, Y1, C2, C3` 均位于顶层。
- `U1` 到 `Y1` 器件原点距离约 `320.2 mil`。
- `C2`、`C3` 到 `Y1` 器件原点距离均约 `150 mil`。
- `HSE_IN`：约 `392.8 mil`，18 段，`8 mil` 线宽，0 过孔，顶层。
- `HSE_OUT`：约 `279.5 mil`，12 段，`8 mil` 线宽，0 过孔，顶层。

工程未提供来自所选 MCU/晶振数据手册或参考设计的版本化约束记录，也未提供负载电容、ESR、驱动电平、起振裕量、地/禁布策略与噪声隔离的人工审阅证据。因此晶振回路状态为 `UNVERIFIED FOR FABRICATION`；以上距离和线长不能单独证明合格。

### 4. PCB 显式走线存在 3 个未说明闭环

新版基线审计按网络把直线、折线、同层交点和过孔建成铜连接图，并以
`E - V + C` 计算独立回环数。电源网和地网不会自动豁免。

- `3V3`：93 个节点、93 条边、1 个连通分量，独立回环数 `1`。
  回环位于顶层，包围框约为 `x=1170..1250 mil`,
  `y=520..560 mil`，8 段，总长约 `222.153 mil`。
- `GND`：124 个节点、124 条边、2 个连通分量，独立回环数 `2`。
  两个回环均位于底层：
  - `x=1450..1660 mil`, `y=650..740 mil`，8 段，约 `547.279 mil`；
  - `x=1410..1450 mil`, `y=620..650 mil`，5 段，约 `116.569 mil`。

这些回环不属于铺铜展开产生的图边，而是显式走线自身闭合。默认应删除
多余支路，使每个网络保持无环连接树。只有确实需要环形供电或并联馈电，
并已评估均流、压降、可靠性和回流路径时，才可使用
`--allow-routing-cycle NET --exception-note TEXT` 逐网络保留。

审计尚未把铺铜和焊盘内部铜展开为图边，因此只能证明上述显式回环存在，
不能据此证明其他网络绝对无回环。

## 已通过的自动检查

### PCB

- EasyEDA PCB DRC：0 错误。
- 17 个 PCB 器件均有位号，无重复位号。
- 4 条板框线构成一个闭合矩形：
  - `1968 × 1378 mil`
  - 约 `49.99 × 35.00 mm`
- 325 条直线走线、0 条圆弧走线。
- 未发现非 0°/45°/90° 的直线走线。
- 1 个 `GND_BOTTOM` 铺铜已生成，有有效实心填充，未保留孤岛。
- 32 个过孔。
- 未发现高速/受控阻抗候选网络；当前分类为 `BASELINE`。

### 原理图元数据复核

- 17 个真实 `part` 类型器件均有位号和封装。
- 自动 JSON 报告中的“24 个缺位号/封装”是基线脚本把图纸和 `3V3/GND` 网络标识计入器件造成的误报，不应作为设计缺陷。
- MCU 电源、地、复位、BOOT 和 SWD 相关引脚未出现在悬空引脚警告中；仍需按连接意图和数据手册人工确认。

## 可制造性观测值

- 最小信号线宽：`8 mil`（约 `0.203 mm`）。
- `3V3` 最小/最大走线宽度：`10/20 mil`。
- `GND` 最小/最大走线宽度：`10/14 mil`。
- 最小过孔外径/孔径：`24/12 mil`（约 `0.610/0.305 mm`）。
- 最小过孔环宽：`6 mil`（约 `0.152 mm`）。

这些数值仅为几何读回。没有指定板厂工艺、电流、允许温升、铜厚和电压条件，不能据此判定载流、温升、压降、间距或制造裕量合格。

## 尚未核验

- 精确未布线数量与原理图/PCB 网表等价性。
- 板框自交、机械尺寸、公差、安装孔、连接器朝向和外壳干涉。
- 器件极性、Pin 1、丝印可读性和装配可访问性。
- 3D 预览、Gerber、钻孔、BOM、坐标文件、面别和旋转。
- 电源完整性、热设计、载流能力和回流路径质量。
- 晶振电气参数、起振裕量与抗干扰能力。

## 建议顺序

1. 修正 4 处重复网络名。
2. 核实 32 个未使用 MCU 引脚与 `SW1.4`，放置 No Connect 标识或补齐连接。
3. 运行器件标准化，修复全部 17 个器件的供应商属性不匹配。
4. 补齐 12 个器件的 BOM Value 字段。
5. 按所选 STM32F103C8T6 与 Y1 的数据手册建立晶振约束记录，重新运行晶振专项审计。
6. 删除 `3V3` 的 1 个和 `GND` 的 2 个多余闭环；若有意保留，逐网补充
   明确的工程理由和审计例外。
7. 重新运行原理图/PCB DRC，并完成未布线、3D、Gerber、钻孔、BOM 和 PnP 人工发布检查。
